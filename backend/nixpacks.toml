[phases.build]
commands = ['pip install -r requirements.txt', 'python manage.py collectstatic --noinput', 'python manage.py migrate']

[start]
cmd = 'gunicorn config.wsgi:application --bind 0.0.0.0:$PORT'
